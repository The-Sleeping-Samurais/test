{{#each books as |book|}}

  <div class="row">
    <div class="col-xs-12">
      <h5>{{book.title}}</h5>
      <h6>By {{book.author}}</h6>
      <p>Book-id: {{book.id}}</p>
    </div>
  </div>

{{/each}}
